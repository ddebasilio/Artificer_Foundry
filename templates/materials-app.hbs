<div class="gatherer-layout">

    {{!-- LEFT PANEL — Material Catalog --}}
    <div class="panel gatherer-catalog-panel">
        <div class="panel-header">
            <h2><i class="fas fa-hammer"></i> Material Catalog</h2>
        </div>

        <div class="gatherer-filters">
            <input class="materials-search" type="text" placeholder="Search materials…" value="{{searchQuery}}">
            <select class="materials-filter-type">
                <option value="all" {{#if (eq filterType "all")}}selected{{/if}}>All Types</option>
                {{#each allTypeLabels}}
                    <option value="{{this.value}}" {{#if (eq ../filterType this.value)}}selected{{/if}}>{{this.label}}</option>
                {{/each}}
            </select>
            <select class="materials-sort">
                <option value="name" {{#if (eq sortBy "name")}}selected{{/if}}>Sort: Name</option>
                <option value="type" {{#if (eq sortBy "type")}}selected{{/if}}>Sort: Type</option>
            </select>
        </div>

        <div class="gatherer-catalog-list">
            {{#each materials}}
                <div class="material-catalog-item ingredient-catalog-item"
                     data-name="{{this.name}}"
                     data-mat-type="{{this.type}}"
                     draggable="true"
                     title="{{this.name}} — {{this.typeLabel}} ({{this.cost}} gp)">
                    <img class="catalog-icon" src="{{this.icon}}" alt="">
                    <div class="catalog-item-info">
                        <span class="catalog-name">{{this.name}}</span>
                        <span class="ingredient-type-badge type-{{this.type}}">{{this.typeLabel}}</span>
                    </div>
                    <span class="catalog-cost">{{this.cost}} gp</span>
                    <div class="catalog-add-controls">
                        <input class="catalog-qty" type="number" min="1" max="99" value="1">
                        <button class="add-material-btn icon-btn" data-name="{{this.name}}" data-mat-type="{{this.type}}" title="Add to inventory">
                            <i class="fas fa-plus"></i>
                        </button>
                    </div>
                </div>
            {{else}}
                <p class="empty-list">No materials found.</p>
            {{/each}}
        </div>
    </div>

    {{!-- RIGHT PANEL — Inventory --}}
    <div class="panel gatherer-inventory-panel">
        <div class="panel-header">
            <h2><i class="fas fa-toolbox"></i>
                {{#if actor}}{{actor.name}}'s Materials{{else}}Material Storage{{/if}}
            </h2>
        </div>

        <div class="gatherer-inventory materials-inventory">
            <div class="gatherer-drop-hint">
                <i class="fas fa-hand-point-left"></i> Drag materials here to add them
            </div>

            {{#if inventory.length}}
                <ul class="gatherer-inventory-list">
                    {{#each inventory}}
                        <li class="gatherer-inv-item">
                            <img class="item-img" src="{{this.img}}" alt="{{this.name}}">
                            <span class="item-name">{{this.name}}</span>
                            <div class="inv-qty-controls">
                                <button class="inv-qty-btn icon-btn" data-item-id="{{this.id}}" data-delta="-1" title="Remove one">−</button>
                                <span class="item-qty">{{this.quantity}}</span>
                                <button class="inv-qty-btn icon-btn" data-item-id="{{this.id}}" data-delta="1" title="Add one">+</button>
                            </div>
                            <button class="inv-remove-btn icon-btn" data-item-id="{{this.id}}" title="Remove all">
                                <i class="fas fa-trash-alt"></i>
                            </button>
                        </li>
                    {{/each}}
                </ul>
            {{else}}
                <div class="gatherer-empty">
                    <i class="fas fa-box-open fa-2x"></i>
                    <p>No materials yet. Use the catalog to add some!</p>
                </div>
            {{/if}}
        </div>
    </div>

</div>
